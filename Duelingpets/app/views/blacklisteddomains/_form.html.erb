<%= form_with(model: @blacklisteddomain, local: true) do |form| %>
  <% if @blacklisteddomain.errors.any? %>
    <div id="errorexplan">
      <h2 id="errorheader"><%= pluralize(@blacklisteddomain.errors.count, "error") %> prohibited this blacklisteddomain from being saved:</h2>

      <ul>
      <% @blacklisteddomain.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="pageformat">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>
  <div class="pageformat">
    <%= form.label :domain_only %>
    <%= form.check_box :domain_only %>
  </div>
  <div class="pageformat">
    <%= form.submit class: "button submitbutton" %>
  </div>
  <br>
  <p class="formlinks"><%= link_to 'Back', blacklisteddomains_path %></p>
<% end %>
