<%= form_with(model: @blacklistedname, local: true) do |form| %>
  <% if @blacklistedname.errors.any? %>
    <div id="errorexplan">
      <h2 id="errorheader"><%= pluralize(@blacklistedname.errors.count, "error") %> prohibited this blacklistedname from being saved:</h2>

      <ul>
      <% @blacklistedname.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="pageformat">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>
  <div class="pageformat">
    <%= form.submit class: "button submitbutton" %>
  </div>
  <br>
  <p class="formlinks"><%= link_to 'Back', blacklistednames_path %></p>
<% end %>
