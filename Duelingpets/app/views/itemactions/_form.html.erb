<%= form_with(model: @itemaction, local: true) do |form| %>
  <% if @itemaction.errors.any? %>
    <div id="errorexplan">
      <h2 id="errorheader"><%= pluralize(@itemaction.errors.count, "error") %> prohibited this itemaction from being saved:</h2>

      <ul>
      <% @itemaction.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="pageformat">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>
  <br>
  <div class="pageformat">
    <%= form.submit class: "button submitbutton" %>
  </div>
  <br>
  <p class="formlinks"><%= link_to 'Back', itemactions_path %></p>
<% end %>
