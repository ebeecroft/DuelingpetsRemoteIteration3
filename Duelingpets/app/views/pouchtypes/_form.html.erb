<%= form_with(model: @pouchtype, local: true) do |form| %>
  <% if @pouchtype.errors.any? %>
    <div id="errorexplan">
      <h2 id="errorheader"><%= pluralize(@pouchtype.errors.count, "error") %> prohibited this pouchtype from being saved:</h2>

      <ul>
      <% @pouchtype.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="pageformat">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>
  <div class="pageformat">
    <%= form.submit class: "button submitbutton" %>
  </div>
  <br>
  <p class="formlinks"><%= link_to 'Back', root_path %></p>
<% end %>
